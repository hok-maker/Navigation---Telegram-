# ================================
# 🚀 Navigation (Next.js) 启动命令
# ================================

# ⚠️ 重要：首次启动或代码修改后，必须先构建！
cd /Volumes/.990Pro_macbook/Cursor工作区/996007工作区/Navigation
npm run build

# ================================
# 📦 开发模式（支持热重载）
# ================================
npm run dev          # 开发服务器：http://localhost:3528

# ================================
# 🚀 生产模式（PM2启动，推荐）
# ================================

# 方式1: 使用 ecosystem.config.js（推荐）
pm2 start ecosystem.config.js

# 方式2: 从主目录统一启动所有程序
cd /Volumes/.990Pro_macbook/Cursor工作区/996007工作区
pm2 start ecosystem.config.js

# ================================
# 🔧 重新部署（修改代码/配置后）
# ================================

# 完整重新部署流程
cd /Volumes/.990Pro_macbook/Cursor工作区/996007工作区/Navigation && pm2 stop Navigation && pm2 delete Navigation && pm2 flush && rm -rf .next && npm run build && pm2 start ecosystem.config.js
-------
pm2 stop Navigation
pm2 delete Navigation
pm2 flush
rm -rf .next
npm run build
pm2 start ecosystem.config.js
# ================================
# 📊 日常管理
# ================================


# 查看日志
pm2 logs Navigation

# 查看状态
pm2 status Navigation

# 重启（不重新构建）
pm2 restart Navigation

# 停止
pm2 stop Navigation

# ================================
# 📝 其他命令
# ================================

# 安装依赖
npm install

# 修复依赖漏洞
npm audit fix

# 创建数据库索引
npm run create-indexes
